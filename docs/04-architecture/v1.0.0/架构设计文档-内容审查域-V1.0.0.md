# 内容审查域 - 架构设计文档 V1.0.0

> **关联PRD**：`01-requirements/16-内容审查域-PRD-v1.0.0.md`（✅已冻结）
> **关联设计**：`03-design/v1.0.0/内容审查/设计文档-内容审查-v1.0.0.md`（✅已冻结，反向检查已通过）
> **架构师**：Arch Agent（AI辅助）
> **设计日期**：2026-07-22
> **V1核心策略**：纯前端模拟（Sim模式），不对接腾讯云API，架构预留real适配器接口

---

## 第一章：NFR非功能需求识别（Step 0）

### 1.1 识别方法

基于PRD中6个FN进行8大NFR主题逐项分析，标注AI置信度和Arch确认决策。

### 1.2 NFR清单

| NFR主题 | 识别的需求点 | 来源FN | AI置信度 | Arch决策 | 实现方式 |
|---|---|---|---|---|---|
| **异步处理** | 模拟回调数据由定时器异步生成（5-15s间隔），通过Store事件广播，不阻塞UI渲染 | FN-AUDIT-INFRA-001 | 95% | ✅ 确认 | `setInterval` 定时器 + Pinia `$subscribe` |
| **异步处理** | 回放擦音任务模拟（5-10s进度条），异步不阻塞页面交互 | FN-AUDIT-PC-004 | 90% | ✅ 确认 | `Promise` + 进度状态机 |
| **事件驱动** | 擦音事件由Store广播 → 直播中控追加列表 + 观众端触发效果 | FN-AUDIT-INFRA-001/PC-002/APP-001 | 95% | ✅ 确认 | Pinia Store `$subscribe` + 共享Store模式 |
| **数据一致性** | 共享Store确保直播中控和观众端看到的违规数据一致 | FN-AUDIT-INFRA-001 | 85% | ✅ 确认 | 单一Store实例 + Pinia state共享 |
| **安全** | 操作日志记录（开关变更→记录操作人/时间/新旧状态） | BR-AUDIT-016 | 80% | ✅ 确认 | Sim模式下内存记录，real模式下API记录 |
| **可扩展** | sim/real双适配器模式，V1使用sim，后期切换到real对接腾讯云 | 架构决策 | 100% | ✅ 确认 | Adapter Pattern（`src/adapters/` 五维可插拔） |
| **可用性** | 30%回调丢失场景覆盖（观众端提示，中控不追加记录） | FN-AUDIT-INFRA-001 | 90% | ✅ 确认 | 随机概率 + 条件分支 |
| **可用性** | 擦音处理超时（30s）→ 标记超时+可手动重试 | FN-AUDIT-PC-004 | 85% | ✅ 确认 | `setTimeout` + 超时状态 |
| **可用性** | 擦音处理失败（10%概率）→ 展示原片+警告标注 | FN-AUDIT-PC-004 | 85% | ✅ 确认 | 随机概率 + 错误状态 |
| **性能** | 违规列表实时刷新（Store推送→追加列表，不重新渲染整个列表） | FN-AUDIT-PC-002 | 80% | ✅ 确认 | Vue响应式更新 + 虚拟滚动（列表>50条时） |
| **性能** | 违规列表筛选排序在前端完成（数据量V1模拟 < 100条） | FN-AUDIT-PC-002 | 90% | ✅ 确认 | `computed` 属性 + Array filter/sort |
| **状态管理** | 场次状态机（直播中→已结束→回放中）+ 违规状态机（待处理→已记录/已断流/已忽略/已超时/已归档） | BR-AUDIT-008/017 | 95% | ✅ 确认 | 状态机契约（`contracts/state-machine/`） |

### 1.3 NFR分级结论

| 级别 | 数量 | 说明 |
|---|---|---|
| ✅ 确认实现 | 12项 | Sim模式下前端可独立实现 |
| ⏸️ 延迟到real | 2项 | 安全审计（操作日志持久化）+ 性能（大量数据） |

---

## 第二章：架构总览

### 2.1 五层架构（自上而下）

```
┌──────────────────────────────────────────────┐
│  页面层 (Pages)                              │
│  运营后台 / 租户后台 / 观众端H5              │
├──────────────────────────────────────────────┤
│  组件层 (Components)                         │
│  通用组件 + 业务组件                         │
├──────────────────────────────────────────────┤
│  存储层 (Stores - Pinia)                     │
│  auditStore（违规/处置/擦音配置/状态共享）    │
├──────────────────────────────────────────────┤
│  服务层 (Services)                           │
│  auditService / replayService / mockGenerator │
├──────────────────────────────────────────────┤
│  适配器层 (Adapters - sim/real)              │
│  data / transport / stream / asset / auth     │
└──────────────────────────────────────────────┘
         ↕ 契约层 (Contracts - Zod)
         实体Schema / API契约 / 状态机
```

### 2.2 数据流

```
┌─────────────────────────────────────────────────────────────┐
│ [定时器] MockGenerator (SimAdapter)                          │
│    │ 每 5-15s 生成违规事件                                    │
│    ▼                                                        │
│ auditService.handleViolation(event)                          │
│    │ 1. 30%概率标记回调丢失                                   │
│    │ 2. 写入 auditStore.violations[]                          │
│    ▼                                                        │
│ auditStore (Pinia - 共享Store)                               │
│    │ $subscribe                                              │
│    ├──→ PC直播中控：追加违规列表 + 告警统计更新               │
│    └──→ H5观众端：触发擦音效果（静音/擦音动画）               │
│                                                              │
│ [用户操作] PC直播中控处置                                      │
│    │ 记录/断流/忽略                                           │
│    ▼                                                        │
│ auditService.handleDisposal(violationId, type, reason)       │
│    │ 更新 violation.disposal_status                           │
│    │ 若断流：场次状态 → 已结束，广播断流事件                   │
│    ▼                                                        │
│ auditStore → $subscribe → H5观众端显示"直播因违规已终止"      │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 路由规划（严格对齐 PRD §17「三终端路由/入口复用」）

**前缀规则**：`/admin/` = 运营后台 | `/tenant/` = 租户后台 | `/h5/` = 观众端

| 路由 | 终端 | 页面 | 关联FN | PRD §17 来源 |
|---|---|---|---|---|
| `/admin/tenant` | PC-运营后台 | 租户管理列表 →「内容审查开关」弹窗（PG-AUDIT-PC-001） | FN-AUDIT-PC-001 | PRD §17 行1 |
| `/tenant/dashboard` | PC-租户后台 | 仿真入口：直播列表模拟（PG-ENTRY-TENANT-001） | —（仿真入口页，非PRD路由） | 仿真需要 |
| `/tenant/live-control?tab=audit` | PC-租户后台 | 直播中控 →「内容审查」Tab（PG-AUDIT-PC-002） | FN-AUDIT-PC-002, FN-AUDIT-PC-003 | PRD §17 行3 |
| `/tenant/live/:streamId/violations` | PC-租户后台 | 历史违规列表面板+处置+擦音模式（PG-AUDIT-PC-004） | FN-AUDIT-PC-002, FN-AUDIT-PC-003 | PRD §17 行2+行5 |
| `/tenant/live/:streamId/replay` | PC-租户后台 | 回放详情 → 擦音处理（PG-AUDIT-PC-003） | FN-AUDIT-PC-004 | PRD §17 行4 |
| `/h5/live/:roomId` | H5-APP | 观众端直播间（PG-AUDIT-APP-001） | FN-AUDIT-APP-001 | PRD §17 行6 |

**FN路由全覆盖验证**：
- FN-AUDIT-INFRA-001：共享基础设施，由MockGenerator驱动，无独立路由（嵌入SimAdapter）
- FN-AUDIT-PC-001 ✅ → `/admin/tenant`（租户管理列表中弹窗）
- FN-AUDIT-PC-002 ✅ → `/tenant/live-control?tab=audit` + `/tenant/live/:streamId/violations`
- FN-AUDIT-PC-003 ✅ → `/tenant/live/:streamId/violations`（违规处置+擦音模式在侧滑面板内完成）
- FN-AUDIT-PC-004 ✅ → `/tenant/live/:streamId/replay`
- FN-AUDIT-APP-001 ✅ → `/h5/live/:roomId`

---

## 第三章：三层契约（C-A5）

### 3.1 实体Schema（Zod）

详见 `src/contracts/schemas/audit-schemas.ts`：
- `ReviewViolationSchema`（违规记录 — ENT-AUDIT-001）
- `ReviewDisposalSchema`（处置记录 — ENT-AUDIT-002）
- `TenantAuditConfigSchema`（租户审查配置 — ENT-AUDIT-003）
- `ReplayMuteTaskSchema`（回放擦音任务 — ENT-AUDIT-004）
- `ReplayFileSchema`（回放文件 — ENT-AUDIT-005）

### 3.2 API契约

详见 `src/contracts/api/audit-api.ts`：
- `POST /api/audit/switch` — 租户审查开关
- `GET /api/audit/violations/:liveId` — 获取违规列表
- `POST /api/audit/disposal` — 违规处置
- `GET /api/audit/replay/:liveId/mute-status` — 回放擦音状态

### 3.3 状态机

详见 `src/contracts/state-machine/audit-state-machine.ts`：

**场次状态机**：
```
直播中 ──[断流处置]──→ 已结束
直播中 ──[正常结束]──→ 已结束
已结束 ──[回放生成]──→ 回放中
```

**违规状态机**：
```
待处理 ──[记录]────→ 已记录
待处理 ──[断流]────→ 已断流
待处理 ──[忽略]────→ 已忽略
待处理 ──[30s超时]─→ 已超时
待处理 ──[审查关闭]─→ 已归档
```

---

## 第四章：Store架构

### 4.1 auditStore（Pinia Store）

```typescript
// 共享Store — PC直播中控 + H5观众端 共用
useAuditStore {
  // 状态
  violations: ReviewViolation[]        // 违规列表（实时追加）
  tenantConfig: TenantAuditConfig       // 租户审查配置
  muteMode: 'silent' | 'beep'          // 擦音/静音模式
  fieldStatus: 'live' | 'ended'        // 场次状态
  callbackLost: boolean                 // 当前是否有回调丢失
  replayTask: ReplayMuteTask | null     // 回放擦音任务

  // 计算属性
  alertStats: { l1: number, l2: number, l3: number, l4: number }
  pendingViolations: ReviewViolation[]
  filteredViolations(filter: FilterOptions): ReviewViolation[]

  // 操作
  appendViolation(v: ReviewViolation): void
  setMuteMode(mode: 'silent' | 'beep'): void
  disposeViolation(id: string, type, reason): Promise<void>
  setFieldStatus(status: 'live' | 'ended'): void
  startReplayMuteTask(streamId): Promise<void>
  reset(): void  // 清理（页面卸载时）
}
```

---

## 第五章：Mock后端（C-A9）

基于Zod Schema自动生成Mock路由（Sim模式下）：

| 路由 | 方法 | 请求体 | 响应 | 模拟逻辑 |
|---|---|---|---|---|
| `/api/audit/switch` | POST | `{ tenantId, enabled }` | `{ success, config }` | 更新内存配置，记录操作日志 |
| `/api/audit/violations/:liveId` | GET | — | `{ violations[], stats }` | 返回内存中违规列表（SimAdapter数据） |
| `/api/audit/disposal` | POST | `{ violationId, type, reason }` | `{ success, disposal }` | 更新违规状态，触发Store广播 |
| `/api/audit/replay/:liveId/mute-status` | GET | — | `{ task, progress }` | 返回擦音任务状态 |

Mock后端入口：`backend-examples/fastapi/main.py`

---

## 第六章：跨端验收脚手架（C-A10）

### 6.1 e2e-acceptance/ 目录结构

```
e2e-acceptance/
├── scenarios/
│   ├── sc-audit-001-switch.spec.ts       # 租户审查开关
│   ├── sc-audit-002-silent-mute.spec.ts  # 实时擦音-静音
│   ├── sc-audit-003-beep-mute.spec.ts    # 实时擦音-擦音
│   ├── sc-audit-004-disposal.spec.ts     # 直播中控处置
│   ├── sc-audit-007-cutoff.spec.ts       # 断流提示
│   ├── sc-audit-008-callback-lost.spec.ts # 回调丢失
│   └── sc-audit-009-history.spec.ts      # 历史违规入口
├── shared/
│   ├── test-context.ts                   # 共享数据上下文
│   └── utils.ts                          # 公共工具函数
└── reports/                              # 验收报告输出目录
```

### 6.2 验收场景清单

| 场景编号 | 场景名 | 终端 | 验证点 |
|---|---|---|---|
| SC-AUDIT-001 | 租户审查开关 | PC-运营后台 | 开关弹窗→确认→生效 |
| SC-AUDIT-002 | 实时擦音-静音 | H5-APP | 🔇静音2-5s→恢复 |
| SC-AUDIT-003 | 实时擦音-擦音 | H5-APP | 🎧嘀声2-5s→恢复 |
| SC-AUDIT-004 | 直播中控处置 | PC-租户后台 | 处置→状态更新→广播 |
| SC-AUDIT-007 | 断流提示 | H5-APP | 结束提示+音频停止 |
| SC-AUDIT-008 | 回调丢失 | H5-APP | 30%触发→橙色警告 |

---

## 第七章：架构决策记录（ADR）

| ADR编号 | 决策 | 理由 | 替代方案 |
|---|---|---|---|
| ADR-001 | sim/real双适配器模式 | V1纯前端模拟，V2+对接腾讯云，适配器模式零修改切换 | 条件编译（侵入性强） |
| ADR-002 | 单一Pinia Store共享状态 | 直播中控+观众端需要实时共享违规数据，避免多Store同步问题 | EventBus（调试困难） |
| ADR-003 | Zod契约先行 | 运行时校验+类型推导，架构→FD→QA全链路一致性 | JSDoc注释（无运行时校验） |
| ADR-004 | Mock后端采用Python FastAPI | 与Vue前端解耦，便于后续替换为真实后端，团队Python技能 | Express.js（前端全栈一致性但V1策略不需要） |
| ADR-005 | Element Plus组件库 | PRD明确复用已有页面风格，SAAS项目全线使用Element Plus | Ant Design Vue（迁移成本） |
| ADR-006 | 前端直接模拟定时器+Store事件 | V1不需要任何后端，纯内存运行，降低依赖 | Mock Service Worker（额外依赖，过度设计） |

---

## 第八章：DIR树——架构产物文件清单

本次架构阶段需要产出的文件：

```
src/
├── contracts/
│   ├── schemas/
│   │   └── audit-schemas.ts           ← 5个实体Zod Schema（✅已实现）
│   ├── api/
│   │   └── audit-api.ts               ← 4个API契约（✅已实现）
│   └── state-machine/
│       └── audit-state-machine.ts     ← 场次状态机+违规状态机+回放擦音状态机（✅已实现）
│   └── index.ts                       ← 统一导出入口（Zod Schema + API + StateMachine re-export）
├── stores/
│   └── audit-store.ts                 ← Pinia Store（共享状态中心，✅已实现）
├── services/
│   └── audit-service.ts               ← 违规检测+处置+广播+回放擦音逻辑（✅已实现，replay逻辑内嵌）
├── adapters/
│   ├── sim/
│   │   ├── data-adapter.ts            ← Sim数据实现（定时器+随机事件+30%回调丢失，✅已实现）
│   │   ├── transport-adapter.ts       ← Sim传输（Pinia Store事件，✅已实现）
│   │   └── auth-adapter.ts            ← Sim认证（跳过，✅已实现）
│   └── real/
│       ├── data-adapter.ts            ← Real数据模板（HTTP → Mock后端API，✅已实现）
│       ├── transport-adapter.ts       ← Real传输模板（WebSocket，✅已实现）
│       └── auth-adapter.ts            ← Real认证模板（JWT，✅已实现）
├── components/
│   └── audit/
│       ├── operator/                  ← PC-运营后台专属组件（/admin/前缀，禁止混放）
│       │   └── (.vue 待FD实现)
│       ├── tenant/                    ← PC-租户后台专属组件（/tenant/前缀，禁止混放）
│       │   └── (.vue 待FD实现)
│       └── viewer/                    ← H5观众端专属组件（/h5/前缀，禁止混放）
│           └── (.vue 待FD实现)
├── pages/
│   ├── tenant-dashboard/
│   │   └── TenantDashboardEntry.vue   ← 租户后台仿真入口-模拟直播列表（✅已实现）
│   ├── audit-switch/
│   │   └── AuditSwitchPage.vue        ← 运营后台-租户管理列表+审查开关弹窗 /admin/tenant（✅已实现）
│   ├── live-control/
│   │   └── LiveControlAuditPanel.vue  ← 租户后台-直播中控审查面板 /tenant/live-control?tab=audit（✅已实现）
│   ├── violations/
│   │   └── ViolationsPanel.vue        ← 租户后台-历史违规列表面板 /tenant/live/:streamId/violations（✅新增）
│   ├── replay/
│   │   └── ReplayDetailAudit.vue      ← 租户后台-回放详情审查 /tenant/live/:streamId/replay（✅已实现）
│   └── viewer/
│       └── AudienceLiveRoom.vue       ← 观众端直播间 /h5/live/:roomId（✅已实现）
├── router/
│   └── index.ts                       ← 路由配置（✅已对齐PRD §17）
└── main.ts                            ← 应用入口

backend-examples/fastapi/
└── main.py                            ← Mock后端（待实现）
```

---

> **下一步**：Arch Review Checkpoint — PM审核架构设计（红线C-A1~C-A13），通过后流转FD（前端开发）。
